[project]
name = "download"
version = "0.1.0"
description = "One-time model download for claire/ and offline/ subprojects"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub",
    "pyannote.audio>=3.1",
    "torch",
    "torchaudio",
    "truststore",
    "whisperx",
]
